{% comment %} <script>
    trackLinksRegisterLinkText('product-grid-item--link', 'BM Featured Product Clicked')
    trackLinksRegisterLinkText('collection-grid-item__link', 'BM Featured Collection Clicked')
    trackEvent("BM Redesign Loaded")
    addMouseFlow()
    mixpanel.track('New Redesign Homepage loaded')
</script> {% endcomment %}
{{ content_for_index }}
