import React from "react"
import CheckoutAddPromo from "./"

export default {
  title: "Checkout/components/CheckoutAddPromo",
  component: CheckoutAddPromo,
}

const Template = (args) => <CheckoutAddPromo {...args} />

export const Default = Template.bind({})
Default.args = {}
