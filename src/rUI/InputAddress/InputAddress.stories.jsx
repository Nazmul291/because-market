import React from "react"
import InputAddress from "./"

export default {
  title: "rUI/InputAddress",
  component: InputAddress,
}

const Template = (args) => <InputAddress {...args} />

export const Default = Template.bind({})
Default.args = {}
